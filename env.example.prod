# BaseVitale – Variables pour livrable Ben (docker-compose.prod)
# Copier en .env à la racine. « Zéro config » : valeurs par défaut ci‑dessous.
# docker compose -f docker-compose.prod.yml up -d

# -----------------------------------------------------------------------------
# API (port exposé)
# -----------------------------------------------------------------------------
PORT=3000

# -----------------------------------------------------------------------------
# PostgreSQL (interne au compose)
# -----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=basevitale

# -----------------------------------------------------------------------------
# Neo4j (interne au compose)
# -----------------------------------------------------------------------------
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j

# -----------------------------------------------------------------------------
# Redis (interne au compose)
# -----------------------------------------------------------------------------
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# Mode IA : MOCK | LOCAL | CLOUD
# - MOCK : pas d’appel Python, données factices (démo sans Ollama/OpenAI).
# - LOCAL : sidecar Python (ai-cortex) + Ollama. OLLAMA_BASE_URL requis si Ollama hors Docker.
# - CLOUD : Groq ou OpenAI. GROQ_API_KEY ou OPENAI_API_KEY requis.
# -----------------------------------------------------------------------------
AI_MODE=MOCK

# -----------------------------------------------------------------------------
# LOCAL (AI_MODE=LOCAL) – Ollama
# Si Ollama sur la machine hôte (Mac) : host.docker.internal.
# Si Ollama dans le même compose : http://ollama:11434/v1.
# -----------------------------------------------------------------------------
OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
OLLAMA_MODEL=llama3.2

# -----------------------------------------------------------------------------
# CLOUD (AI_MODE=CLOUD) – Groq ou OpenAI
# -----------------------------------------------------------------------------
# GROQ_API_KEY=gsk_...
# OPENAI_API_KEY=sk-...
# CLOUD_LLM_PROVIDER=groq
# GROQ_MODEL=llama-3.1-8b-instant
# OPENAI_MODEL=gpt-3.5-turbo

# -----------------------------------------------------------------------------
# CORS (origines autorisées pour l’API). Défaut *.
# -----------------------------------------------------------------------------
# CORS_ORIGIN=https://app.ben.com
