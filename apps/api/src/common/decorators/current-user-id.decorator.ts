/**
 * CurrentUserId Decorator - Alias for compatibility
 * 
 * Ce fichier est un alias pour maintenir la compatibilité avec les imports existants.
 * Le décorateur réel est défini dans current-user.decorator.ts
 */

export { CurrentUserId } from './current-user.decorator';
